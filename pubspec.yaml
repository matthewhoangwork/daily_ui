name: daily_ui
description: "Chu ky hinh thuc chunky (app/ daily-vocab) — dung chung cho moi app Flutter."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
